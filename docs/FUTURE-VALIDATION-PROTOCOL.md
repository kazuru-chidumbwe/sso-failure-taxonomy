# Future validation protocol (not manuscript study evidence)

**Purpose:** Answer reviewer requests for independent checking **without** claiming inter-rater reliability on unconstrained narratives in the current paper (Path D lock).

## Closed-book specification packet (recommended first pilot)

| Element | Scope |
| --- | --- |
| Materials | S1–S10 decision-test vignettes + R1–R2 rejected-estate vignettes only |
| Task | Assign one primary Table IV label or `outside_taxonomy` using Fig. 2 walk + tie-breakers |
| Coders | One external IAM/SSO practitioner; author packet withheld until after coding |
| Metrics | Per-item agreement with frozen golden map; qualitative notes on ambiguous boundaries |
| Out of scope | N=11 estate rows; private I4/I5 tickets; prevalence |

Golden labels: `codebook/stress-cases.json`, `codebook/rejected-estate-vignettes.json`, verified by `make smoke`.

## Field diagnostic study (later)

- Measure time-to-primary-label and misroute rate vs pre-instrument baseline on redacted tickets/logs.
- Requires multi-organization data and IRB/DP review where applicable.
- Not part of the v1.0.27 artifact evaluation model.

## Community falsifiability (ongoing)

`docs/EXTERNAL-INCIDENTS.md` — submissions scored `fits` / `fits_with_clarification` / `forces_new_label`; not N=11 evidence.
