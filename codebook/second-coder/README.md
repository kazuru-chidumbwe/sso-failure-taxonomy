# Independent coding packet (exploratory archive)

> **Manuscript note (Path D, Sep 2026):** This structured-packet pilot and `reliability.json` are **not cited as study evidence** in the IEEE Access manuscript. Evaluation in the paper uses the S1–S8 decision-test battery and `validate_catalog_assignments.py` instead.

Send [`PACKET.md`](PACKET.md) as a **standalone file**. Do not point the coder at this repository until they have submitted.

`codebook/incidents.json` in this repository **is** the published catalog (category, severity, and form for all eleven rows, plus header counts). A second-coder round against a public catalog is therefore **unblinded**. The working protocol is still “packet only until submit” so the sheet is not a transcription exercise.

The task was agreement on label assignment given Table IV, not independent taxonomy discovery. n=11 across ten categories yields wide agreement intervals; archival summary: [`../reliability.json`](../reliability.json).
