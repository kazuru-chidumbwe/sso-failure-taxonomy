# Pattern — edge OIDC relying party (redacted)

Placeholder templates for an edge OpenID Connect path that grants network access after identity success. Not a production config.

Runnable F3/F5 nonce demo: [`../../harness/`](../../harness/).

All public files must pass the fingerprint test in [`docs/SCOPE.md`](../../docs/SCOPE.md). No hostnames, secrets, site IDs, or organisation names.
